/* eslint-disable react/prop-types */


const TopService = ({service}) => {
    // let set =  service.sort((a, b) => (b?.starRatings) - (a?.starRatings));
    // console.log(set);
    // return (
    //     <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    // {/* let set =  service.sort((a, b) => (b?.starRatings) - (a?.starRatings)); */}
        
    </div>
  );
};


export default TopService;